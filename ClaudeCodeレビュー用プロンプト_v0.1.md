# Claude Code レビュー用プロンプト v0.1

設計書 v0.2 に基づく P0〜P7 全フェーズ実装完了後の総括レビューを Claude Code に依頼するためのプロンプト集。

---

## 投入方針

- **本体プロンプト（総括レビュー）**：1セッションで網羅レビューを行う。最初にこれを投入する。
- **オプション分割プロンプト（A〜D）**：本体で深掘りが必要と判断された領域のみ追加投入する。

---

## 本体プロンプト：総括レビュー

```
あなたは本リポジトリ（C:\app\okozukai）の独立レビュアーである。
以下を必ず守ること。

【最重要：カスタムNext.js】
1. C:\app\okozukai\AGENTS.md を読む
2. node_modules/next/dist/docs/ 内の関連ドキュメントを参照する
3. 既存実装パターンを最低3つ参照する
標準Next.jsの記憶でAPIや挙動を判断しない。

【レビュー対象】
設計書「C:\app\okozukai\お小遣い制度設計書_v0.2.md」に基づき、
プロンプト集「C:\app\okozukai\ClaudeCode投入用プロンプト集_v0.1.md」の P0〜P7 を実装した結果を総括レビューする。
ユーザーは「全プロセス完了」と申告しているが、申告を疑い、完了基準の達成を独立検証すること。

【レビュー方針】
- 静的解析（コードリーディング）と動的確認（実行による挙動確認）の両方を行う
- 「修正必須（仕様違反・バグ・セキュリティ）」と「改善提案（UX・運用・性能）」を厳格に分離する
- 改善提案には実装コスト見積（S=半日／M=1〜2日／L=3日以上）を必ず付与する
- 不明点は推測で書かず「要確認」とする
- 解決策の押し付けを行わず、判断材料を提示する

【レビュー観点（7軸）】

A. 仕様準拠性
   - schema.prisma が設計書 §4.2 と一致しているか（モデル名・カラム名・型・enum値）
   - 定数値が設計書通りか（60分=500円、手数料500円、60分単位限定）
   - 項目seedが設計書 §7 と一致しているか（おてつだい31件／べんきょう3件／マイナス33件＋PROPORTIONAL1件）
   - 「床そうじ」分数の暫定値の扱い（注記が残っているか）

B. データ整合性
   - TimeLedger / YenLedger が単調増加のイベントログとして実装されているか（更新・削除されていないか）
   - 残高は集計（sum）で算出されているか、それともキャッシュ列に依存しているか
   - 換算・換金・申請承認の各操作がトランザクションで包まれているか
   - トランザクション失敗時のロールバックが機能するか
   - マイナス残高の境界条件（残高=0／-1／500未満／500以上）の挙動

C. 認可・セキュリティ
   - 親のみ可能な操作（項目CRUD・ペナルティ計上・換金承認）が子から実行不可か
   - 他家族のデータが参照できない実装になっているか（familyId による絞り込み）
   - 認証ミドルウェア（src/lib/auth.ts）が全エンドポイントで一貫適用されているか
   - 棄却理由の必須バリデーションが API 層で機能しているか（フロントだけでないか）

D. UX・アクセシビリティ
   - スマホ幅 375px でレイアウト崩れがないか
   - マイナス残高が視覚的に判別できるか（赤色強調等）
   - 未就学児（杜輝・雪凪）が使う場合の配慮（読み上げ／絵文字／文字サイズ）
   - 棄却理由が子の画面で見やすく表示されているか
   - 換金画面に金融教育的なヒント（手数料率・推奨換金額）があるか
   - 申請承認画面に一括処理の余地があるか

E. パフォーマンス
   - Ledger 集計のクエリにインデックスが効いているか（[userId, occurredAt]）
   - ホーム残高表示の N+1 問題（家族4人分の集計が一括取得か個別か）
   - 履歴一覧のページネーション有無
   - フロント側のキャッシュ戦略（SWR／React Query 等）と revalidate

F. テスト網羅性
   - ユニットテストの存在と対象（残高計算、override 解決、PROPORTIONAL 計算）
   - 境界値テスト（0分換算／60未満換算／残高0換金／マイナス時換金）
   - 棄却理由空文字テスト（API が 400 を返すか）
   - E2E テストの有無

G. 金融教育の実装
   - 換金画面に「次に X 円ためれば実質受取 Y 円」の表示があるか
   - 手数料率の可視化（換金額に対する 500円 の割合表示）
   - マイナス時の換金不可理由の表示が分かりやすいか

【動的確認シナリオ（必ず実行すること）】

ブラウザ操作またはAPI呼び出しで以下のシナリオを実行し、期待結果と一致するか検証する。
不一致があれば「修正必須」に分類する。

シナリオ1：勉強時間申請→承認→残高反映
  1. 子で 30分 勉強時間申請
  2. 親で承認
  3. 子のホームで時間残高が +30分 されている
  4. TimeLedger に該当レコードが1件追加されている

シナリオ2：お手伝い申請→棄却→棄却理由表示
  1. 子で「茶わん洗い」を1回申請
  2. 親で棄却（理由：「やり方が雑」）
  3. 子の申請一覧で REJECTED と棄却理由が表示される
  4. TimeLedger に追加レコードが「ない」ことを確認

シナリオ3：ペナルティ計上（FIXED）→残高減算
  1. 親で「ご飯を残す」（-10分）を3回計上
  2. 子のホームで時間残高が -30分 されている
  3. TimeLedger に -30分 のレコードが1件追加

シナリオ4：時間→金額換算（60分単位制限）
  1. 子で 30分 換算を試みる → API が 400 を返す
  2. 子で 60分 換算 → 成功、時間残高 -60分、金額残高 +500円
  3. TimeLedger と YenLedger の両方に1件ずつ記録

シナリオ5：換金（マイナス時不可）
  1. 残高 < 500円 の状態で換金実行 → API が 400 を返す
  2. 残高 ≧ 500円 で換金 → grossYen 減算、CashOut 1件追加

シナリオ6：マイナス残高での消費可
  1. 残高 -100分 の状態でゲーム時間 30分 を消費登録
  2. 登録成功し、残高が -130分 になる

シナリオ7：比例モードペナルティ
  1. 「決められた時間以降に話す」（単位 -20分）を超過 5分 で計上
  2. TimeLedger に -100分 が記録される

シナリオ8：他家族データへのアクセス遮断
  1. 別家族のユーザーIDで /api/study-logs?userId=X を叩く
  2. 403 または空配列が返る

【出力フォーマット】

# レビュー結果報告書

## 1. 完了基準の達成状況
- P0〜P7 各フェーズの完了基準（プロンプト集 v0.1 記載）に対する達成可否を ✓ / ✗ / 部分達成 で表
- 未達のものは未達理由を1行で

## 2. 修正必須項目（バグ・仕様違反・セキュリティ）
| # | 観点 | 該当ファイル:行 | 現象 | 期待値 | 推奨修正 |
|---|---|---|---|---|---|

## 3. 動的確認シナリオ結果
| シナリオ | 結果（PASS/FAIL） | 備考 |
|---|---|---|

## 4. 改善提案（優先度付き）
| # | 観点 | 提案内容 | 優先度（高/中/低） | 実装コスト（S/M/L） | 関連ファイル |
|---|---|---|---|---|---|

## 5. 仕様未確定論点（要確認）
- 設計書 v0.2 §11 の未確定論点について、実装でどう扱われたかを列挙

## 6. ロールバック性
- 直近マイグレーションの戻し手順
- 重大な不具合発見時の暫定回避策

## 7. 総合所見
- 本番運用に進めるか、追加修正が必要かの判定
- 追加修正が必要な場合の最小スコープ

【禁止事項】
- 推測で「動いているはず」と書かない。動的確認シナリオを必ず実行する
- 改善提案で過剰な機能追加を提案しない（家庭運用での実装維持コストを考慮）
- 既存設計書 v0.2 を勝手に書き換える提案をしない（v0.3 候補として別記する）

【出力前のセルフチェック】
[ ] 動的確認シナリオ8件をすべて実行したか
[ ] 修正必須と改善提案を厳格に分離したか
[ ] 改善提案に実装コスト見積を付与したか
[ ] 推測ではなく実コード／実挙動に基づくか
```

---

## オプション分割プロンプト

総括レビューの結果、特定領域で深掘りが必要な場合に追加投入する。

### 分割A：データ整合性深掘り

```
（共通前提を貼る）

総括レビューで指摘された TimeLedger / YenLedger 周りのデータ整合性を深掘り検証する。

■ 検証項目
1. 並行リクエスト時の整合性
   - 同一ユーザーへの「換算」と「換金」が同時実行された場合の挙動
   - prisma の $transaction の isolationLevel 設定
2. 過去データ補正（ADJUSTMENT）の運用想定
   - 親が誤計上を取り消すケースの実装（取消用 ADJUSTMENT を追加するか、論理削除か）
3. 残高 reconciliation
   - 仮にキャッシュ列を導入した場合の不整合検知ジョブの設計案

■ 出力
- 並行性の問題が起こりうる経路の列挙
- 取消フロー設計案（コード変更 or 運用ルール）
- reconciliation ジョブの実装方針（推奨の有無含む）
```

### 分割B：認可深掘り

```
（共通前提を貼る）

総括レビューで指摘された認可境界の網羅検証を行う。

■ 検証項目
1. 全 API エンドポイント一覧と、各々の認可レベル（PARENT/CHILD/SELF/ANY）
2. 子が他の子のデータを操作できないか（自分のID以外を指定した場合）
3. 親が他家族のデータを操作できないか（familyId 指定改ざん）
4. セッション切れ・トークン期限切れ時の挙動

■ 出力
- API 認可マトリクス表
- 認可漏れの可能性がある経路と、再現方法
- 推奨修正パッチ
```

### 分割C：UX 深掘り（特に未就学児／低学年対応）

```
（共通前提を貼る）

未就学児（杜輝・雪凪）と小3（柚輝）が使う前提でのUX検証を行う。

■ 検証項目
1. 文字読み上げ（TTS）の実装可否と推奨
2. 漢字の使用範囲（小3で読めない漢字をひらがな化する仕組み）
3. ボタンの最小タップ領域（44x44px 以上）
4. 申請フォームの選択数（多すぎる場合のグルーピング案）
5. エラーメッセージの語彙

■ 出力
- 該当画面・コンポーネントごとの問題点
- 改善案（最小コストでの段階改善案）
- 「親のサポートで使う」運用と「子が単独で使う」運用の閾値判断
```

### 分割D：パフォーマンス深掘り

```
（共通前提を貼る）

データ蓄積後のパフォーマンス劣化を予測検証する。

■ 検証項目
1. 1年運用時のレコード数試算
   - TimeLedger（1日10件×4人×365日）
   - ChoreClaim、PenaltyEvent 等
2. 残高集計クエリの EXPLAIN 結果
3. ホーム表示の総クエリ数（家族4人）
4. インデックス設計の妥当性
5. キャッシュ列導入の損益分岐点

■ 出力
- 想定レコード数とクエリ性能の試算
- 改善余地（インデックス追加、ページネーション、キャッシュ列）
- 改善実施の推奨タイミング（運用何ヶ月後等）
```

---

## レビュー結果の活用方針

1. 修正必須項目 → 即時 Claude Code に修正タスクを投入（別プロンプト）
2. 改善提案 → 設計書 v0.3 への取り込み候補として家族で判断
3. 仕様未確定論点 → 家族で意思決定後、設計書 v0.3 に確定値として反映
4. ロールバック手順 → リポジトリの README または運用ドキュメントに転記

---

## 改訂履歴

- v0.1（本書）：初版。総括レビュープロンプト＋分割プロンプト4本

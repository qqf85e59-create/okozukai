# お小遣い制度設計書 v0.2

本ドキュメントは、家庭運用中のお小遣い制度をアプリ要件に落とし込むための設計書である。
Claude Code 等の実装エージェントへの指示書、または別AIによる設計レビュー時の入力として使用する。

v0.1からの主な変更点：
- 未確定論点（換金単位、マイナス残高、棄却フロー、項目編集権限）を確定値に反映
- 項目マスタの個人別設定（家族共通＋個人別override）を追加
- ペナルティに比例計算モード（PROPORTIONAL）を追加（例：超過時間×20分）
- 棄却理由欄を全申請モデルに標準実装
- PDF添付の項目一覧を seed データとして取り込み

---

## 1. 目的

家庭で運用中の「時間を主通貨とするお小遣い制度」をアプリ（C:\app\okozukai／プロダクト名 TOYBOX ARCADE）で完結運用可能にする。

達成すべきこと：
- 時間の獲得・喪失・消費・換金を一元管理する
- 子の申請と親の承認・棄却フローをアプリ上で完結する
- 金額換算・週次換金を手作業ゼロで実行する
- 既存機能（残高表示、ペナルティ、おてつだい項目）を破壊せず段階移行する
- 金融教育的観点（手数料を意識した高額換金行動）を促進する

---

## 2. 用語定義

| 用語 | 定義 |
|---|---|
| 時間残高 | 子が現在保有する時間（分単位、マイナス可） |
| 金額残高 | 時間を換算した仮想お小遣い残高（円、マイナス可） |
| プラス項目 | 時間を加算する事象。勉強時間／お手伝い項目 |
| マイナス項目 | 時間を減算する事象。ペナルティ項目 |
| 申請 | 子がプラス項目を「実施した」と登録する行為 |
| 承認／棄却 | 親が申請に判断を下す行為。棄却時は理由必須 |
| 換算 | 時間残高→金額残高への変換（子が任意タイミング、60分=500円固定） |
| 換金 | 金額残高→現金への変換。週1回、手数料500円。マイナス残高時は不可 |
| 消費 | 時間残高→ゲーム／スマホ等の利用時間に充てる行為。子が計測して登録 |
| 固定モード（FIXED） | 項目の分数 × 回数で計上 |
| 比例モード（PROPORTIONAL） | 項目の単位分数 × 実数値（例：超過分数）で計上 |

なお、現状アプリの「メダル一覧」「プレイ履歴」「ステータス」等のメニュー名はデザイン上のポップ表現であり、本制度上の独立軸ではない。本設計では既存メニューの呼称はそのまま維持し、内部ロジックのみ本書に従って整理する。

---

## 3. 制度の基本ルール（家庭運用：確定）

### 3.1 通貨モデル

- 主通貨：時間（最小単位 1分。UIは10分単位ステッパー推奨）
- 派生通貨：金額（円）。換算レート 60分 = 500円
- 終端通貨：現金。週次換金時に手数料500円を差引いた純額を支給
- 時間・金額ともにマイナス許容（青天井）

### 3.2 時間の獲得（プラス）

| 種別 | 単位 | 登録方法 |
|---|---|---|
| 勉強時間（実時間申請） | 実数（10分単位推奨） | 子が申請 → 親承認 |
| お手伝い | 項目別の固定+分数 × 回数 | 子が項目選択 → 親承認 |
| 学習成果 | 項目別の固定+分数（例：テスト100点で+15分） | 子が申請 → 親承認 |

### 3.3 時間の喪失（マイナス）

| 種別 | 単位 | 登録方法 |
|---|---|---|
| ペナルティ（固定モード） | 項目分数 × 回数 | 親が直接登録（棄却概念なし、即時計上） |
| ペナルティ（比例モード） | 項目単位分数 × 実数（例：超過分数） | 親が直接登録 |

比例モードの代表例：「決められた時間以降に話す」＝ 超過時間×20分。

### 3.4 時間の消費

- ゲーム／スマホ／TV等の利用に時間残高を充てる
- 子が時間を計測して任意のタイミングで登録（自己申告）
- 残高がマイナスでも消費自体は登録可能（青天井）
- 親による事後確認・調整可

### 3.5 時間→金額換算

- 子の任意タイミングで実行可能
- レート：60分 = 500円（固定）
- 入力単位：60分単位限定（30分・90分等は不可）
- 一度換算した分は時間残高から減じ、金額残高に加える

### 3.6 換金（金額→現金）

- 頻度：週1回（現行は土曜午前）
- 実行主体：子が任意のタイミングで申請。親はその場で現金支給
- 手数料：1回あたり500円（金額残高から差引）
- 支給額：金額残高 - 500円
- マイナス残高時：換金不可（残高 < 500円も実質的には換金不可。手数料込みで残高 ≧ 500円が条件）
- 金融教育上の意図：1回ごとに500円固定の手数料が発生するため、頻繁換金より高額一括換金が経済合理的になる設計。UIで「次にこの金額を貯めれば実質受取○円」のヒント表示を検討

### 3.7 マイナス残高の扱い

- 時間・金額ともにマイナスは制度上許容
- マイナス時の制限：換金のみ不可。時間消費（ゲーム等の登録）はマイナスでも可（青天井方針）
- 親への通知：時間・金額のいずれかがマイナスに転落／復帰した瞬間にPush通知

---

## 4. データモデル設計（Prisma想定）

### 4.1 既存モデル

`User`、`SavingsGoal`、`Request`、`CashRequest`、`Comment`、`Push`、`Audit`、`Attachment`。
本設計移行に伴い、`Request`/`CashRequest`/`Comment` の用途が新モデルと重複する場合は段階廃止または用途分離を検討。

### 4.2 追加・拡張モデル

```prisma
// =====================================================================
// 台帳系（イミュータブルなイベントログ）
// =====================================================================

model TimeLedger {
  id           String   @id @default(cuid())
  userId       String
  deltaMinutes Int      // 正：加算 / 負：減算
  reason       LedgerReason
  sourceType   LedgerSourceType
  sourceId     String?
  occurredAt   DateTime
  createdById  String
  approvedById String?
  note         String?
  createdAt    DateTime @default(now())

  @@index([userId, occurredAt])
  @@index([sourceType, sourceId])
}

enum LedgerReason {
  STUDY
  CHORE
  PENALTY
  CONVERT_OUT
  CONSUME
  ADJUSTMENT
}

enum LedgerSourceType {
  STUDY_LOG
  CHORE_CLAIM
  PENALTY_EVENT
  TIME_CONVERT
  TIME_CONSUME
  ADJUSTMENT
}

model YenLedger {
  id         String   @id @default(cuid())
  userId     String
  deltaYen   Int
  reason     YenLedgerReason
  sourceType String?
  sourceId   String?
  occurredAt DateTime
  createdAt  DateTime @default(now())

  @@index([userId, occurredAt])
}

enum YenLedgerReason {
  CONVERT_IN
  CASH_OUT
  CASH_FEE
  BONUS
  EXPENSE_DEDUCT
  ADJUSTMENT
}

// =====================================================================
// 項目マスタ（家族共通）と個人別override
// =====================================================================

model ChoreItem {
  id           String   @id @default(cuid())
  familyId     String
  category     String?  // 例：おてつだい / べんきょう
  name         String
  bonusMinutes Int      // 家族共通のデフォルト分数
  mode         ItemMode @default(FIXED)
  active       Boolean  @default(true)
  sortOrder    Int      @default(0)
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt

  @@index([familyId, active])
}

model UserChoreOverride {
  id           String  @id @default(cuid())
  userId       String
  choreItemId  String
  bonusMinutes Int?    // null=家族共通分数を使用、値あり=個人別分数
  active       Boolean @default(true)

  @@unique([userId, choreItemId])
}

model PenaltyItem {
  id             String   @id @default(cuid())
  familyId       String
  name           String
  penaltyMinutes Int      // 家族共通のデフォルト（正の値で保持、計上時に符号反転）
  mode           ItemMode @default(FIXED) // FIXED=分数×回数 / PROPORTIONAL=単位分数×実数
  unitLabel      String?  // 比例モード時のUI表示。例：「超過分数」「件数」
  active         Boolean  @default(true)
  sortOrder      Int      @default(0)
  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt

  @@index([familyId, active])
}

model UserPenaltyOverride {
  id             String  @id @default(cuid())
  userId         String
  penaltyItemId  String
  penaltyMinutes Int?
  active         Boolean @default(true)

  @@unique([userId, penaltyItemId])
}

enum ItemMode {
  FIXED         // minutes × count
  PROPORTIONAL  // minutes × actualValue（例：超過分数）
}

// =====================================================================
// 申請・実施イベント
// =====================================================================

model StudyLog {
  id             String      @id @default(cuid())
  userId         String
  minutes        Int         // 実数申請
  studiedAt      DateTime
  note           String?
  status         ClaimStatus @default(PENDING)
  approvedById   String?
  approvedAt     DateTime?
  rejectedReason String?     // 棄却時の理由（必須運用）
  createdAt      DateTime    @default(now())
  updatedAt      DateTime    @updatedAt

  @@index([userId, studiedAt])
  @@index([userId, status])
}

model ChoreClaim {
  id              String      @id @default(cuid())
  userId          String
  choreItemId     String
  count           Int         @default(1)
  actualValue     Int?        // PROPORTIONALモード用
  totalMinutes    Int         // 計上時にスナップショット保存
  performedAt     DateTime
  note            String?
  status          ClaimStatus @default(PENDING)
  approvedById    String?
  approvedAt      DateTime?
  rejectedReason  String?
  createdAt       DateTime    @default(now())
  updatedAt       DateTime    @updatedAt

  @@index([userId, status])
}

model PenaltyEvent {
  id             String   @id @default(cuid())
  userId         String
  penaltyItemId  String
  count          Int      @default(1)
  actualValue    Int?     // PROPORTIONALモード用（例：超過分数）
  totalMinutes   Int
  occurredAt     DateTime
  reason         String   // 親による事由記述（必須運用）
  createdById    String
  createdAt      DateTime @default(now())

  @@index([userId, occurredAt])
}

model TimeConvert {
  id           String   @id @default(cuid())
  userId       String
  minutesUsed  Int      // 60分単位限定
  yenGained    Int      // (minutesUsed / 60) * 500
  rateSnapshot Int      @default(500)
  convertedAt  DateTime @default(now())

  @@index([userId, convertedAt])
}

model TimeConsume {
  id          String          @id @default(cuid())
  userId      String
  minutesUsed Int
  category    ConsumeCategory
  consumedAt  DateTime        @default(now())
  note        String?

  @@index([userId, consumedAt])
}

enum ConsumeCategory {
  GAME
  SMARTPHONE
  TV
  OTHER
}

model CashOut {
  id           String   @id @default(cuid())
  userId       String
  grossYen     Int
  feeYen       Int      @default(500)
  netYen       Int
  cashedOutAt  DateTime @default(now())
  approvedById String   // 親
  note         String?

  @@index([userId, cashedOutAt])
}

enum ClaimStatus {
  PENDING
  APPROVED
  REJECTED
}
```

### 4.3 残高算出方針

- 時間残高 = `sum(TimeLedger.deltaMinutes WHERE userId=X)`
- 金額残高 = `sum(YenLedger.deltaYen WHERE userId=X)`
- いずれもLedger集計を正とし、`User`等への残高キャッシュは設けない（不整合リスク回避）
- ホーム表示の高速化が必要になった段階で、reconciliationジョブ付きのキャッシュ列を導入検討

### 4.4 個人別override の解決ロジック

```
function resolveBonusMinutes(userId, choreItemId):
  override = UserChoreOverride.findUnique(userId, choreItemId)
  if override?.bonusMinutes != null:
    return override.bonusMinutes
  return ChoreItem.findUnique(choreItemId).bonusMinutes
```
PenaltyItem も同様。`active=false` のoverrideは個人別に項目自体を非表示にする用途で使用。

---

## 5. 主要ユースケース

### 5.1 子：勉強時間を申請（実時間）
1. 子が `/study-logs/new` で日時・分数・メモ入力
2. `StudyLog` を `PENDING` で作成、親へPush通知
3. 親が承認 → `TimeLedger` に `+minutes` 計上
4. 親が棄却時は `rejectedReason` 必須、子へPush通知

### 5.2 子：お手伝いを申請（固定モード）
1. 子が項目選択＋回数入力
2. `totalMinutes` = `resolveBonusMinutes(userId, itemId) × count` をスナップショット
3. `ChoreClaim` を `PENDING` で作成、親承認で `TimeLedger` 計上

### 5.3 子：学習成果を申請（固定モード／例：テスト100点）
お手伝いと同フロー。カテゴリ「べんきょう」配下の項目として管理。

### 5.4 親：ペナルティを計上（固定モード）
1. 親が項目選択＋対象児＋回数＋事由入力
2. `totalMinutes` = `resolvePenaltyMinutes × count`
3. `PenaltyEvent` 作成、`TimeLedger` に `-totalMinutes`、子へPush通知

### 5.5 親：ペナルティを計上（比例モード／例：超過時間×20分）
1. 親が項目選択＋対象児＋実数（例：超過分数）＋事由入力
2. `totalMinutes` = `resolvePenaltyMinutes × actualValue`
3. `PenaltyEvent` 作成、`TimeLedger` 計上

### 5.6 子：時間を金額化
1. 子が60分単位で入力（30分・90分等は無効）
2. `TimeConvert` 作成、`TimeLedger` に `-minutes`、`YenLedger` に `+yen`

### 5.7 子：時間を消費（ゲーム／スマホ）
1. 子が利用時間を計測して登録
2. `TimeConsume` 作成、`TimeLedger` に `-minutes`
3. マイナス残高でも登録可

### 5.8 子：換金を申請＋親が現金支給
1. 子が `/cashout/new` で金額確認画面へ
2. 残高 ≧ 500円 を確認、不足時は換金ボタン無効化＋理由表示
3. 確定で `CashOut` 作成、`YenLedger` に `-grossYen`（feeは内訳記録）
4. 親はその場で純額（grossYen - 500円）を現金で渡す
5. 「次回○円までためれば手数料率○%」と次回換金推奨額をUIで表示

### 5.9 親：臨時収入（お年玉等）
- `YenLedger` に `BONUS` で `+yen` を即時記録

### 5.10 親：実費控除
- `YenLedger` に `EXPENSE_DEDUCT` で `-yen` を即時記録

### 5.11 親：項目マスタ管理
- `/admin/chore-items`／`/admin/penalty-items` でCRUD（親のみ）
- 個人別override画面：子ごとに分数を上書きできる管理画面

---

## 6. 画面・機能要件（差分）

### 6.1 ホーム
- 「みんなの残高」を「時間残高（h:m）／金額残高（円）」の二段表示に変更
- 子ログイン時：自分の残高を大きく表示、自分の承認待ち件数バッジ
- 親ログイン時：全員の残高一覧、承認待ち件数バッジ、最新ペナルティ一覧
- マイナス残高は赤色強調

### 6.2 申請（子）
- `/study-logs/new`：勉強時間申請
- `/chore-claims/new`：項目選択＋回数（または実数）
- `/requests`：自分の申請ステータス一覧（PENDING/APPROVED/REJECTED）。棄却理由表示

### 6.3 承認（親）
- `/approvals`：承認待ち一覧
- 承認／棄却ボタン。棄却時は理由入力モーダル必須

### 6.4 ペナルティ（親）
- `/penalties/new`：項目選択＋対象児＋（回数 or 実数）＋事由
- 既存「実費控除」（円）は別画面として残置（YenLedger側）

### 6.5 換算・消費・換金
- `/convert`：60分単位ステッパーで換算
- `/consume`：消費登録（カテゴリ選択＋分数）
- `/cashout`：換金画面。残高条件・手数料・純額表示。次回推奨額表示

### 6.6 マスタ管理（親）
- `/admin/chore-items`：CRUD、カテゴリ別表示、ドラッグソート
- `/admin/penalty-items`：CRUD、モード切替（FIXED/PROPORTIONAL）
- `/admin/users/[id]/overrides`：子ごとに項目分数のオーバーライド設定／項目非表示

### 6.7 履歴
- `/ledger`：時間台帳・金額台帳の一覧、期間／対象児／種別フィルタ
- 子は自分のみ、親は全員

---

## 7. 項目マスタ初期データ（PDF添付反映）

### 7.1 ChoreItem（カテゴリ：おてつだい）

| 分数 | 項目 |
|---|---|
| +60 | おつかい マツキヨになくて、もう1件行った場合 |
| +60 | 床水ぶきそうじ（全部屋・ろう下・階段・洗面所セット） |
| +50 | おつかい ビッグ・イオン |
| +50 | 床そうじ（全部屋・ろう下・階段・洗面所セット） |
| +40 | おふろそうじ（有み戸もセット） |
| +40 | 窓ふき・さっしそうじ（全各階） |
| +30 | おつかい マツキヨ |
| +22 | 洗面所そうじ |
| +20 | おふろそうじ（タイル・さっし・カビ箇所） |
| +15 | ときせつのおふろしたく（服ぬがす・保湿・ドライヤー・服着・服準備） |
| +15 | ソファ・カーペットコロコロ（ソファカバーセットまで） |
| +15 | トイレそうじ |
| +15 | 茶わん洗い |
| +15 | 洗たくもののたたみ |
| +15 | 洗たくものの干し |
| +10 | げんかんそうじ |
| +10 | ときせつのおむつ換え（うんち） |
| +10 | ときせつのおふろ |
| +10 | カーテン取る・つける（1カ所） |
| +10 | 普段のおふろそうじ |
| +10 | ミルク（げっぷまで） |
| +10 | ときの歯みがき |
| +10 | 米とぎ |
| +10 | ときせつのおもちゃ整理整頓 |
| +5 | カーテン開け・閉め |
| +5 | テーブルふき |
| +5 | ときせつのおむつ換え（おしっこ） |
| +5 | ごみすて |
| +5 | 朝ごはんを作ってあげる |
| +5 | ときの寝るしたく（スリーパー・ベットに連れていく） |
| +5 | ときのトイレ |

「床そうじ」が +50 か +40 かはPDF判読要確認。

### 7.2 ChoreItem（カテゴリ：べんきょう）

| 分数 | モード | 項目 |
|---|---|---|
| +15 | FIXED | テスト100点 |
| +8 | FIXED | テスト90点以上 |
| +1（単位） | PROPORTIONAL | 勉強時間（実時間申請）※実数分数で加算 |

### 7.3 PenaltyItem（カテゴリ：マイナス）

| 分数 | モード | 項目 |
|---|---|---|
| -600 | FIXED | ウソのぼうりく |
| -600 | FIXED | 勝手に動画を見る・やる |
| -300 | FIXED | ときを起こす |
| -300 | FIXED | 借金5万円以上で外出禁止を破った |
| -60 | FIXED | 朝8時前にときせつが寝ている間にバスケをする |
| -60 | FIXED | 世話を引き受けたにも関わらず最後までやりとげない |
| -60 | FIXED | 歯をみがかない |
| -60 | FIXED | ウソをつく |
| -60 | FIXED | 暴力 |
| -60 | FIXED | モノにあたる（人のモノ） |
| -60 | FIXED | 許可を取らずに通信機器を2Fに持って行く |
| -60 | FIXED | 人につば・唾をかける |
| -30 | FIXED | 石を投げる |
| -30 | FIXED | モノにあたる（自分のモノ） |
| -30 | FIXED | 人のせいにする |
| -20 | FIXED | 夜8時までに子供部屋の整理整頓が行われていない（1つにつき） |
| -20 | FIXED | 出したものを片付けない（1つにつき） |
| -10 | FIXED | 決めつけた |
| -10 | FIXED | 最初に「ちがう」と言った |
| -10 | FIXED | 突発的にウソをつく |
| -10 | FIXED | 同じ事を3回注意された |
| -10 | FIXED | 下着を着ないで洗面所から出る |
| -10 | FIXED | カーテン（子供部屋）・マットレス立てかけていない |
| -10 | FIXED | 走り回ってはいけない所で走り回る |
| -10 | FIXED | 片手でご飯を食べる |
| -10 | FIXED | 足を立てて食べる |
| -10 | FIXED | イス・ソファの上に立つ |
| -10 | FIXED | ご飯を残す |
| -10 | FIXED | 時間オーバー（登校・習い事・寝る時間）1個につき |
| -10 | FIXED | 人の物を勝手に使う |
| -10 | FIXED | 勉強の答えを教え合う |
| -10 | FIXED | くつのかかとをふむ |
| -10 | FIXED | ご飯中に立ち歩く |

### 7.4 PenaltyItem（新規追加）

| 分数（単位） | モード | 項目 | 単位ラベル |
|---|---|---|---|
| -20 | PROPORTIONAL | 決められた時間以降に話す（超過時間×20分） | 超過分数 |

PROPORTIONALモードの追加項目は親がマスタ管理画面から自由に増設可能。

---

## 8. 現状アプリとのGAP

| # | 既存 | 不足／変更 | 対応方針 |
|---|---|---|---|
| 1 | 円ベース「みんなの残高」 | 時間残高表示 | TimeLedger 集計の追加表示 |
| 2 | 「+臨時収入」モーダル | （バグ）残高反映されない | バグ修正（先行タスク） |
| 3 | おてつだい31件マスタ | 申請→承認フロー、+分数設定、個人別override | ChoreItem/ChoreClaim/UserChoreOverride |
| 4 | ペナルティ「実費控除」 | 時間ペナルティ計上、比例モード、個人別override | PenaltyItem/PenaltyEvent/UserPenaltyOverride |
| 5 | なし | 勉強時間（実時間申請） | StudyLog |
| 6 | なし | 時間→金額換算（60分単位） | TimeConvert |
| 7 | なし | 時間消費（ゲーム/スマホ） | TimeConsume |
| 8 | 週次集計ボタン | 換金（手数料500円、マイナス時不可） | CashOut |
| 9 | なし | 棄却理由欄 | 全申請モデルにrejectedReason |
| 10 | なし | 項目CRUD（親のみ） | マスタ管理画面 |

---

## 9. 残高反映バグとの関係

「+臨時収入」登録後に残高が0円のままという既知バグがある。
本設計の Ledger 方式で残高は「`YenLedger` 集計」として導出されるため、バグ修正と本設計移行は同一改修で吸収可能。

推奨：
1. まず既存実装でのバグ箇所を特定し、最小修正で残高反映を回復（短期）
2. その後、Phase 1 で `TimeLedger`／`YenLedger` 基盤を導入し、既存の残高ロジックを Ledger 方式に置換（中期）

---

## 10. 確定事項（v0.1からの確定）

| 論点 | 確定値 |
|---|---|
| 時間→金額換算の単位 | 60分単位限定、60分=500円固定 |
| 時間消費の記録方法 | 子が計測して登録（自己申告） |
| マイナス残高時の換金 | 不可（残高 ≧ 500円 が条件） |
| 換金実行主体 | 子が任意のタイミングで申請、親が現金支給 |
| 換金頻度 | 週1回（土曜午前推奨）／実質的には子の任意 |
| 申請棄却フロー | 親が理由必須で棄却。プラス・マイナスとも理由欄を実装 |
| 項目編集権限 | 親のみ |
| 項目分数の個人別設定 | 家族共通＋個人別overrideで対応 |
| 項目追加・削除 | 親が任意に可能 |
| ペナルティの比例計算 | PROPORTIONALモードで対応（例：超過時間×20分） |

---

## 11. 残る未確定論点

| # | 論点 | 想定選択肢 |
|---|---|---|
| 1 | 比例モードの実数入力上限 | 上限なし／項目別に上限設定 |
| 2 | 申請の取下げ | 子は PENDING のものを取下げ可能か |
| 3 | 承認後の修正・取消 | 親が事後修正できるか／監査ログ前提で可とするか |
| 4 | 残高マイナス転落時の通知 | 親のみ／子のみ／両方 |
| 5 | 子のログイン認証 | 個別アカウント／家族共通の子モード／PIN |
| 6 | 換金の「土曜午前固定」UI制限 | 曜日ロックする／推奨表示のみ／無制限 |
| 7 | 既存データ移行 | 既存「みんなの残高」円残高をBONUSとしてYenLedgerに一括投入する／ゼロ起算 |
| 8 | プッシュ通知の集約頻度 | 即時／5分集約／1時間集約 |

---

## 12. 実装優先順位（提案）

| Phase | 内容 | 完了条件 |
|---|---|---|
| P0 | 残高反映バグ修正 | 「+臨時収入」「実費控除」が即時残高反映する |
| P1 | TimeLedger/YenLedger 基盤＋既存残高のLedger化 | ホームの残高表示がLedger集計値で動作 |
| P2 | ChoreItem/PenaltyItem マスタ＋個人別override＋seed投入 | 親が項目CRUD可能、PDF項目が初期投入済 |
| P3 | StudyLog/ChoreClaim/PenaltyEvent と申請承認フロー（棄却理由含む） | 子の申請→親の承認・棄却が完結 |
| P4 | TimeConvert（60分単位）＋ホームに時間残高表示 | 子が時間→金額変換可能 |
| P5 | CashOut（手数料500円・マイナス時不可・推奨表示） | 子が換金申請、親が現金支給フロー完結 |
| P6 | TimeConsume（ゲーム/スマホ消費登録） | 子が消費登録可能 |
| P7 | PROPORTIONALモード対応（例：超過時間×20分） | 比例モードのペナルティ計上動作 |

各Phase完了時に家族試用30分。違和感・運用矛盾があれば次Phaseの優先順位を再評価。

---

## 13. 想定読者と用途

- 想定読者：実装エージェント（Claude Code等）／別AIによる設計レビュー／家族（運用者）
- 用途：実装着手前の合意形成、別AIへの引継書、段階リリース時の優先順位判断材料

---

## 改訂履歴

- v0.1：初版。家庭運用ルールの言語化＋データモデル骨子＋未確定論点の列挙
- v0.2（本書）：未確定論点の確定値反映、個人別override追加、PROPORTIONALモード追加、棄却理由欄標準実装、PDF項目一覧をseedとして反映
